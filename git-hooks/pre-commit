#!/bin/sh
# pre-commit — delegates to git-autocommit-block
# This file must be named exactly "pre-commit" for git to invoke it.
# The actual logic lives in git-autocommit-block (same directory).

SCRIPT_DIR="$(dirname "$0")"
exec "$SCRIPT_DIR/git-autocommit-block"
